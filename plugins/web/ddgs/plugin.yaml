name: web-ddgs
version: 1.0.0
description: "DuckDuckGo web search via the ddgs Python package — no API key required. Install with `pip install ddgs`."
author: tanzeelshujahkhan
kind: backend
provides_web_providers:
  - ddgs
